function cartesianProduct(...a) {
    return a.reduce((a, b) => a.flatMap(d => b.map(e => [d, e].flat())));
}

function calculateSetBonuses(equippedArtifacts, checkedGameplayFlags) {
    let addedSettypeStats = null;
    addedSettypeStats = "test";
    return addedSettypeStats
}

function calculateTotalDamage(characterBaseStats, addedArtifactStats, addedWeaponStats, addedSettypeStats) {
    let totalDamage = 0;
    return totalDamage;
}